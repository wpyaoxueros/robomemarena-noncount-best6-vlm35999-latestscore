# VLA+VLM Codex Outbox

## 2026-08-24 Task1 high_vlm_v2 + VLA35999 no-hold evaluation

- Status: `PREPARED_NOT_RUN`
- Intent: copy `jwen341/openpi_rm` VLM evaluation logic into this repository and run Task1 seed104 with VLA checkpoint 35999.
- Control contract: VLM supplies the primitive prompt; VLA supplies every action; no hold, release, anchor, oracle prompt injection, or GT replay.
- Source policy: `/data/user/jwen341/openpi_rm` is read-only. All source/runtime copies live under `high_vlm_v2_task1_vla35999_nohold/`.
- Planned evidence: source and runtime-asset SHA-256 manifests, borrowed-account probes, Slurm identity, exact command, summary, prompt trace, and main/wrist video paths.
- Result: not yet available.
- Source validation: all 18 copied source/runtime files are byte-identical to the read-only source tree; `SOURCE_MANIFEST.sha256` passes.
- No-hold audit: `PASS`; the runner uses `--action-source vla`, and no hold/release/anchor/oracle control flags are present.
- Preflight failure: the first copied CLI import failed because the initial inventory omitted `primitive_weighted_loss.py` and `training_components.py`.
- Fix: copied those two original files byte-for-byte and added them to both source verification gates. The copied evaluator now reaches and prints its CLI help successfully.
- Borrowed-account static write preflight failure: `hzhang061` could read the copied adapter and LIBERO tree but could not write `records/`; the directory inherited `umask 022` and was mode `2755`.
- Permission fix: add a scoped preparation script that sets only this experiment's generated-data roots (`records/`, `runtime_assets/`, `runs/`) to group-writable setgid directories. No source account tree is changed.
- Borrowed-account audit-script failure: the `hzhang061` login environment has no `rg` executable, so the first static no-hold audit exited `127` after read/write checks had passed.
- Portability fix: replace `rg` in the audit script with system `grep -E/-F`; the evaluated source and contract are unchanged.
- Formal request: `task1_seed104_nohold_20260824_010402`, Unix user `hzhang061`, fresh 1-GPU probe then fresh 2-GPU request-shape probe, followed by one Task1 seed104 episode only if both pass.
- Probe result: fresh 1-GPU Job `537542` and fresh 2-GPU Job `537543` passed on `ACD1-11`; formal Job `537544` started on `acd_u` with two H100 80GB GPUs.
- Formal startup issue: the run-local `LIBERO_CONFIG_PATH` was empty, so LIBERO paused for its first-run dataset-path question before environment creation.
- One-time continuation: sent `N` to initialize LIBERO's default paths under the copied runtime. This changed no model, prompt, action, stage, or scoring behavior.
- Reproduction fix: formal runner now writes that same run-local `config.yaml` before importing LIBERO, eliminating interactive input in future runs.
- Invalid formal result: Job `537544` reached Task1 step 245, completed physical `pick cookies`, autonomously switched to `place cookies into basket`, then the evaluator exited by `SIGABRT`; no episode summary or video was produced, so this is not counted as a model result.
- Launcher integrity issue: the shared runner was edited while Job `537544` was active, corrupting the parent shell's post-child parse after the evaluator abort. Future launches copy an immutable runner and probe into the run request's probe directory before Slurm submission.
- Frozen-runner retry: fresh probe Jobs `537680`/`537681` passed; formal Job `537682` used commit `2746740` and frozen runner SHA-256 `87beaf3cb9f2b1c20b58008d9592bdc9ea786e5f88b6d4a71965bbbe7ea44342`.
- Retry result: Job `537682` crossed the prior failure point, reached step 250, then the evaluator again exited by `SIGABRT` (exit 134). No summary/video was produced; the run is invalid, not a model failure.
- Root-cause gate: run a one-GPU, 300-step VLM plus environment diagnostic using original HDF action replay. This preserves the VLM call/image path but removes the VLA server and autonomous action trajectory; it is diagnostic-only and cannot count as success.
- Diagnostic request `task1_gt_replay_runtime_diag_20260824_015802`: no probe or formal allocation started. All three partitions rejected the dynamically discovered 2,000,000 MB maximum-memory request within the immediate gate because matching nodes were busy.
- Resource correction: retain the required maximum-memory attempt first, then fall back to 163,840 MB, which was sufficient for both prior two-model Task1 runs. This changes allocation reachability only.
- Diagnostic retry `task1_gt_replay_runtime_diag_retry1_20260824_020125`: fresh one-GPU Job `537846` and formal-shape Job `537850` passed as Unix user/account `hzhang061` on `acd_ue`; diagnostic Job `537853` ran on `acd_u`, node `ACD1-6`, one H100 80GB, 160 GiB RAM.
- Diagnostic result: `COMPLETED`, exit code `0`; the copied high-vlm-v2 path completed 300 environment steps and 60 synchronous VLM calls, including `t=250..295`, and wrote summary plus main/wrist MP4 files.
- Interpretation: high-vlm-v2, LIBERO, image saving, and repeated VLM inference alone do not reproduce the native abort near step 250. The remaining fault boundary is the removed side of the experiment: the VLA server/action path or the physical state produced by VLA actions.
- Scientific-status guard: this run used original HDF actions (`action_source=gt-replay`, `trajectory_only=true`) and is diagnostic only. Its `task_success=0`/`max_steps` row must not enter Task1 VLA success statistics.
- Next controlled retry: enable only `PYTHONFAULTHANDLER=1` in the experiment-owned runner and rerun the unchanged no-hold autonomous Task1 path. This preserves the byte-identical `jwen341/openpi_rm` source copy while making any repeated evaluator `SIGABRT` produce a component-level stack trace.
- Five-repeat request `task1_nohold_5repeat_20260824_022431`: count the already-running native-trace episode as repeat 0 and launch four additional independent Task1 seed104 repeats in parallel. Every repeat uses high-vlm-v2, VLA checkpoint 35999, no hold/release/anchor/oracle/GT replay, its own two-GPU allocation, port, run directory, and immutable runner snapshot.
- Five-repeat interim result: 4 valid episodes have closed with summaries. Repeat 0 / Job `537892` failed at `2/4` primitives after 2500 steps; repeat 1 / Job `538005` succeeded at `4/4` in 824 steps; repeats 3 and 4 / Jobs `538003` and `538004` failed at `2/4` after 2500 steps. Interim success is `1/4`; every run used Task1 seed104 and the no-hold control contract.
- Resource-gate audit: the first four parallel launch attempts and repeat-2 retry 1 passed fresh borrowed-account probes but did not all receive a formal two-GPU allocation. Attempts without an episode summary are excluded from the denominator and retained only as allocation evidence.
- Fifth valid episode: repeat-2 retry 2 passed fresh one-GPU Job `538041` and two-GPU shape Job `538042`; formal Job `538043` is running on `acd_u`, node `ACD1-31`, with two H100 GPUs.
- Repeat-2 retry 2 terminal result: Job `538043` exited `134` after a native abort in MuJoCo/robosuite `read_pixels`; `summary.tsv` contains only its header and no episode row. It is invalid and excluded, so the valid Task1 aggregate remains `1/4` rather than becoming a five-episode result.

## 2026-08-24 Task2/3/4 high_vlm_v2 physical extension

- Status: `IMPLEMENTED_VALIDATED_LAUNCH_PENDING`.
- User request: keep the Task1 five-repeat evaluation running and start Task2, Task3, and Task4 in parallel.
- Root-cause preflight: copied `eval_three_tasks.py` accepts task IDs 1-26 at the CLI, but autonomous physical evaluation is restricted to Tasks 1/17/20/22; all other tasks are forced into GT-replay `trajectory-only` mode.
- Additional preflight: the immutable copied source contains only the Task1 BDDL, so Task2 resolution fails before environment creation. The tracked official snapshot already contains Task2/3/4 BDDL files and official stage predicates.
- Approved design: leave copied `source/` byte-identical; add an experiment-owned adapter that preserves the exact high-vlm-v2 training prompt and direct VLM-prompt-to-VLA-action loop while injecting only the tracked official `d9f83ac` BDDL and physical stage predicates.
- Scoring: Task2/3 use both official basket-placement stages; Task4 uses official stages 01-08 and keeps final close stage 09 optional, matching the tracked official scorer.
- Initial execution: independent Task2/3/4 seed104 one-episode smoke runs, each with fresh 1-GPU and two-GPU request-shape probes under `hzhang061` before formal launch.
- Design: `docs/superpowers/specs/2026-08-24-high-vlm-v2-task234-physical-eval-design.md`.
- Plan: `docs/superpowers/plans/2026-08-24-high-vlm-v2-task234-physical-eval.md`.
- Implementation: `high_vlm_v2_task1_vla35999_nohold/extensions/eval_task234_physical.py` adds only physical task registration, official BDDL resolution, official completion predicates, and auditable state snapshots around the byte-identical copied runner.
- Frozen scorer: RoboMemArena commit `d9f83ac5182e25ad7f0a301a77a0b667f2392df1`; scorer SHA-256 `0ab5e19cb7b90844b86fe04a76facc0364af55f1e841c4754aa675404a318538`.
- Verified stage contract: Task2 has `Place_Butter_Basket` and `Place_Popcorn_Basket`; Task3 has `Place_Cream_Basket` and `Place_Pudding_Basket`; Task4 has official stages 01-08, while `09_Close_Top_Drawer_Final` remains optional.
- Validation: extension tests `5 passed`; launcher/runner/audit scripts pass `bash -n`; all 18 copied source files remain byte-identical to `/data/user/jwen341/openpi_rm`; no-hold audit reports `PASS`.
- Permission-helper failure: the pre-launch helper tried to chmod historical probe directories owned by `hzhang061` and was rejected with `Operation not permitted`; it did not invalidate the group-writable output roots.
- Permission-helper fix: limit recursive chmod to entries owned by the invoking user. A direct `hzhang061` shell check then confirmed `records/` and `runs/` writable and the extension plus checkpoint-local norm readable.
- Launch request: run Task2, Task3, and Task4 seed104 independently and concurrently from the `hzhang061` shell. Each chain must pass a fresh one-GPU probe and fresh two-GPU shape probe before its two-GPU formal episode.
- Request receipt: `high_vlm_v2_task1_vla35999_nohold/records/run_request_task234_seed104_20260824_032523.md`.
- Launch state: all three independent tmux launchers started from the verified `hzhang061` shell with run IDs ending `20260824_032901`. All fresh probe gates passed and formal Jobs `538177` (Task2), `538181` (Task3), and `538180` (Task4) are running on `ACD1-8`, each with two H100 GPUs.
- Complete probes: Task2 Jobs `538171`/`538174`; Task3 Jobs `538172`/`538179`; Task4 Jobs `538173`/`538176`. Every probe reports Unix user/account `hzhang061`, partition `acd_u`, expected GPU count, and bf16 support.
- First rollout evidence: all three high-vlm-v2 models loaded 750/750 weights, connected to independent VLA35999 servers, resolved their tracked official BDDL, and wrote t=0 dual-camera inputs plus one VLM trace. Autonomous initial prompts are Task2 `pick butter`, Task3 `pick cream`, and Task4 `place butter into top drawer`.
